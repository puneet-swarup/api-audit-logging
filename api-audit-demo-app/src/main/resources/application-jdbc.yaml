audit:
  logging:
    storage:
      type: jdbc
    flyway:
      enabled: true

spring:
  datasource:
    url: jdbc:h2:mem:audit-jdbc-demo;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
  jpa:
    hibernate:
      ddl-auto: none

